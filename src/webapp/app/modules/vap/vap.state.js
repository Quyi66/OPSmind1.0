/**
 * VAP 模块路由配置
 * 注意：CVE 列表页面的路由在 applet-router.js 中动态注册
 * 访问路径：#/apw/vap/cve/list (在补丁管理模块内)
 * @author Generated
 */
(function () {
    'use strict';

    // VAP 模块的自定义页面路由已在 applet-router.js 中注册
    // 这里可以添加其他非 applet 相关的路由配置
    angular.module('oplus.vap').config(['$stateProvider', function ($stateProvider) {
        // 预留：如需添加独立于 applet 的路由，可在此配置
    }]);
})();
