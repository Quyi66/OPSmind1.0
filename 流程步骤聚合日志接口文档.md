# 流程步骤聚合日志接口文档（前端对接）

## 1. 背景

流程模板的一个步骤可以配置多个脚本任务，每个任务执行后各自产生一个 `runId`。
此前前端查看步骤输出时只能按单个 `runId` 查询，一次只能看到其中一个任务的日志。

本次后端改造引入 **步骤聚合运行ID（虚拟 runId）**：

```
step_{stepId}
```

前端把原来传 `runId` 的位置改为传 `step_{stepId}`，即可一次性拿到该步骤下 **所有任务合并后的结果与日志**，无需再按单个 runId 逐个查询。

### 兼容性保证

| 场景 | 行为 |
|------|------|
| 传真实 runId（原有调用方式） | 行为完全不变，老页面无需任何改动 |
| 传 `step_{stepId}`，步骤下只有 1 个任务 | 直接返回该任务的**原始结果**：`runId` 字段为真实运行ID，批次名不改写，与按真实 runId 查询的返回完全一致 |
| 传 `step_{stepId}`，步骤下有多个任务 | 返回合并结果，按"任务N"批次标签区分各任务 |

因此前端可以**统一**用 `step_{stepId}` 查询步骤结果，不必区分步骤里有几个任务；单任务步骤拿到的就是真实 runId 的结果，重新执行、运行中进度等依赖真实 runId 的功能不受影响。

### 批次标签规则（多任务区分方式）

聚合返回的数据中，每个任务的输出通过 **批次（batch）标签** 区分，前端现有的按批次分组/分页签展示逻辑可直接复用：

| 场景 | 批次标签 |
|------|---------|
| 步骤下只有 1 个任务 | 保持原批次名不变（如 `default`） |
| 多个任务，任务原批次名为 `default` | `任务1`、`任务2`…（按步骤中任务配置顺序） |
| 多个任务，任务本身有多个批次 | `任务1-批次名`、`任务2-批次名`… |

---

## 2. 接口清单

### 2.1 获取步骤聚合运行结果

```
GET /jao/api/jao/runlogs/{runId}/result
```

即前端现有的"获取运行结果"接口，**路径与响应结构均不变**，仅 `runId` 支持新形式：

| 参数 | 说明 |
|------|------|
| `runId` | 真实 runId（原有行为）或 `step_{stepId}`（聚合查询） |

传 `step_{stepId}` 且步骤下只有 1 个任务时，返回该任务原始的 `RunLogWithData`（`runId` 为真实运行ID）；多任务时返回合并后的 `RunLogWithData`，字段含义：

| 字段 | 类型 | 聚合规则 |
|------|------|---------|
| `runId` | string | 原样返回 `step_{stepId}` |
| `jobType` | string | 取第一个任务的类型（一般为 `script`） |
| `status` | string | 任一任务在 `RUNNING/WAITING/CALLBACK/PAUSED` → `RUNNING`；否则按 `ERROR` > `FAILED` > `INTERRUPTED` > `COMPLETED` 优先级聚合 |
| `startTime` | datetime | 所有任务中最早的开始时间 |
| `endTime` | datetime | 所有任务中最晚的结束时间；仍有任务未结束时为 `null` |
| `detail.batches[]` | array | 所有任务的批次进度按任务顺序拼接，`batch` 字段已按上述规则改写为 `任务N` 等标签 |
| `data[]` | array | 所有任务的结果数据行（ansible 输出 JSON）按任务顺序拼接，`batchId` 同样已改写 |
| `batchIds[]` | array | 改写后的批次标签列表 |
| `statsJson` | string | 各任务统计数值字段逐项求和后的 JSON |
| `error` | string | 各任务错误信息换行拼接，并带 `[任务N]` 前缀；无错误为 `null` |
| `username` | string | 取第一个任务的执行人 |

返回 `404` 的情况：步骤不存在、步骤尚无任何运行记录。

**说明**：`status` 为 `RUNNING` 时前端可沿用现有的自动刷新逻辑轮询本接口。

### 2.2 WebSocket 实时/历史日志（Output 页签）

```
ws://{host}/jao/log/{runId}
```

即前端现有 `ansible-log-viewer` 使用的 WebSocket 地址，`runId` 同样支持 `step_{stepId}`。

消息格式不变：

```json
{"batchId": "任务1", "message": "PLAY [xxx] ****...\n"}
```

- 传 `step_{stepId}` 时，已完成任务的日志逐条推送，`batchId` 为 `任务N` 标签，前端现有"按 batchId 自动分组为多个页签/chip"的逻辑无需修改，会自然形成 任务1 / 任务2 … 多个日志分组；
- 若步骤内仍有任务在运行，该任务走 ata 实时日志转发（batchId 为原始批次名），连接保持直到任务结束；
- 步骤无运行记录时推送一条 `{"batchId":"default","message":"步骤暂无运行记录"}` 后关闭连接。

### 2.3 下载 ansible 原始输出

```
GET /jao/api/jao/runlogs/ansible/{runId}
```

`runId` 支持 `step_{stepId}`，返回步骤下所有任务原始输出的拼接文本（attachment 下载），多任务时每个任务前有分隔标题：

```
========== 任务1 (e4f1bb9d...) ==========
{ ansible 输出 ... }

========== 任务2 (a2c93d07...) ==========
{ ansible 输出 ... }
```

注意：原始输出来自 ata 滚动日志文件，过期被清理后该任务段会缺失；全部缺失时返回错误。

### 2.4 不变的现有接口（备查）

| 接口 | 说明 |
|------|------|
| `GET /jao/api/jao/flows/steps/{stepId}/result` | 仍返回该步骤各任务 `RunLogWithData` 的**数组**（逐任务、不合并），需要单任务明细时可用 |
| `POST /jao/api/jao/jobs/runlogs`（body 为 runId 数组） | 仍可用于获取各任务的运行记录概要（任务标题、开始时间等），可用来渲染"切换单个任务"下拉框 |

---

## 3. 前端改造建议

涉及 2 个文件、改动很小（oplus-web 仓库）：

### 3.1 `app/modules/udp/widgets/flow-record/flow-record.component.js`

步骤节点点击后向外广播的数据中补充 `stepId`：

```js
// 原：var o = {stepName: e.name, runId: e.runLogIds};
var o = {stepName: e.name, stepId: e.id, runId: e.runLogIds};
```

### 3.2 `app/modules/udp/widgets/flow-instance/flow-instance.component.js`

监听 `that.selectedTaskNodes` 的处理逻辑中，多任务时默认使用聚合 ID，同时保留下拉框用于查看单个任务：

```js
var t = angular.fromJson(n.runId);
// 统一使用步骤聚合ID；后端对单任务步骤自动返回该任务的原始结果，无需前端区分
f.selectedTaskLogId = "step_" + n.stepId;
if (t.length > 1) {
    // 多任务时提供下拉框，可切换查看单个任务
    f.selectedTaskRunLogIds = [];
    l.getRunLogsByRunIds(JSON.stringify(t)).then(function (s) {
        angular.forEach(s, function (s) {
            f.selectedTaskRunLogIds.push({
                id: s.id,
                jobTitle: s.jobTitle,
                startTime: $$.formatDate(s.startTime, "YYYY-MM-DD HH:mm:ss")
            });
        });
        f.selectedTaskRunLogIds = _.sortBy(f.selectedTaskRunLogIds, function (s) { return s.startTime; });
        // 下拉框首项为"全部任务"聚合视图（当前默认选中项）
        f.selectedTaskRunLogIds.unshift({id: "step_" + n.stepId, startTime: "全部任务"});
    });
}
```

`jao-job-result-view`、`ansible-log-viewer`、原始输出下载按钮 **均无需修改**——它们继续拿 `runId`（值为 `step_xxx`）调上述接口即可。

### 3.3 注意事项

1. **重新执行按钮**：概要页签的"重新执行"按钮按 runId 重跑单个作业，对多任务合并视图无效。建议改用**接口返回体里的 `runId` 字段**（单任务步骤返回的是真实运行ID，按钮可正常使用），并在该字段以 `step_` 开头（多任务合并视图）时隐藏按钮。
2. **运行中进度组件**（`jao-ansible-progress`）按真实 runId 查询 ata 进度，对 `step_` ID 查不到数据（静默无显示，不报错）。需要运行中进度时可让用户通过下拉框切到单个任务查看。
3. `step_` 前缀为系统保留约定，真实 runId 是 UUID 不会与之冲突。

---

## 4. 后端改动文件（部署参考）

| 文件 | 改动 |
|------|------|
| `oplus-jao/.../flow/service/StepRunResultService.java` | 新增：步骤聚合结果合并服务 |
| `oplus-jao/.../web/JobRunController.java` | `/runlogs/{runId}/result` 与 `/runlogs/ansible/{runId}` 支持 `step_` 前缀 |
| `oplus-jao/.../socket/server/LogSocketServer.java` | WebSocket `/log/{runId}` 支持 `step_` 前缀聚合推送 |

部署物：重新打包部署 `oplus-svs`（包含 oplus-jao 模块）即可，无数据库变更。
