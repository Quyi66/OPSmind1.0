# Docker构建忽略文件
# 只保留必要的文件用于Docker镜像构建

# 忽略所有文件，然后明确包含需要的文件
*

# 包含必要的文件
!dist/
!nginx.conf
!nginx.conf.tmp
!Dockerfile

# 确保dist目录下的所有文件都被包含
!dist/**

# 排除不必要的文件
.git/
.gitignore
*.md
*.sh
*.log
*.tmp
.DS_Store
Thumbs.db
