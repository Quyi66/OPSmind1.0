# 多租户系统

多租户定义：多租户技术或称多重租赁技术，简称SaaS，是一种软件架构技术，是实现如何在多用户环境下（此处的多用户一般是面向企业用户）共用相同的系统或程序组件，并且可确保各用户间数据的隔离性。简单讲：在一台服务器上运行单个应用实例，它为多个租户（客户）提供服务。

具体到我们系统，租户代表的是一个系统，这个系统有自己独立的访问路径、用户列表和业务数据。租户与租户之间数据隔离。但所有的租户共用一套角色和权限配置。

多租户系统共有两类访问入口：全局唯一的管理入口和每个租户独立的访问入口。管理入口仅供系统超级管理员使用。访问入口供租户内用户使用。


